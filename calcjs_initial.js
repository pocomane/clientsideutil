// Click Run or press ESC to evaluate the following expression:
2 + 2

// Multiple Javascript statements are supported too, e.g.:
//   function fac(x){ if(x<=0){ return 1; }else{ return x*fac(x-1); }};
//   a = fac(3);
//   return Math.PI * fac(a);
